import { TourGenerator } from "@/components/TourGenerator";

/* Server Component: die gesamte Interaktion lebt in der Insel darunter. */
export default function Page() {
  return <TourGenerator />;
}
